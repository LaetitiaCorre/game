class Question {
  /**
   * 
   * @param {*} message Texte de la question 
   * @param {*} answers Liste des 3 réponses possibles
   */
  constructor (message, answers){
    this.message = message;
    this.answers = answers;
  }
}